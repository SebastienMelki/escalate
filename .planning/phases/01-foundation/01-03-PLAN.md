---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .prettierignore
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PLAT-01]
gap_closure: true

must_haves:
  truths:
    - "pnpm run format:check exits 0 with .planning/ directory excluded from Prettier scope"
    - "PLAT-01 is assigned to Phase 2 in both ROADMAP.md and REQUIREMENTS.md traceability"
    - "Phase 1 requirements list in ROADMAP.md no longer includes PLAT-01"
  artifacts:
    - path: ".prettierignore"
      provides: "Prettier exclusion rules"
      contains: ".planning"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 1 requirements without PLAT-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PLAT-01 traceability updated to Phase 2"
  key_links: []
---

<objective>
Close 2 verification gaps from Phase 1: fix Prettier format:check failure by excluding .planning/ directory, and correct PLAT-01 requirement assignment from Phase 1 to Phase 2.

Purpose: Restore format:check as a clean quality gate for downstream phases and fix the planning doc inconsistency before Phase 2 planning begins.
Output: .prettierignore file, updated ROADMAP.md and REQUIREMENTS.md
</objective>

<execution_context>
@/Users/sebastienmelki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastienmelki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .prettierignore excluding .planning directory</name>
  <files>.prettierignore</files>
  <action>
Create a `.prettierignore` file at the project root with the following content:

```
.planning/
```

This excludes the `.planning/` directory from Prettier formatting scope. Planning documents are auto-generated by GSD workflows and should not be constrained by the project's Prettier configuration. This is the recommended fix from verification (preferred over reformatting planning files, which would drift again on next GSD write).

After creating the file, run `pnpm run format:check` to confirm it exits 0.
  </action>
  <verify>Run `pnpm run format:check` -- must exit 0 with no warnings.</verify>
  <done>pnpm run format:check exits 0. The .planning/ directory is excluded from Prettier scope. No source files are excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Move PLAT-01 from Phase 1 to Phase 2 in planning docs</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Two files need targeted edits:

**ROADMAP.md:**
1. In the Phase 1 detail section, change the Requirements line from:
   `**Requirements**: PLAT-01, PLAT-02, PLAT-06, CFG-01, CFG-04`
   to:
   `**Requirements**: PLAT-02, PLAT-06, CFG-01, CFG-04`

2. In the Phase 2 detail section, change the Requirements line from:
   `**Requirements**: IPC-01, IPC-02, IPC-03, IPC-05`
   to:
   `**Requirements**: PLAT-01, IPC-01, IPC-02, IPC-03, IPC-05`

**REQUIREMENTS.md:**
1. In the Traceability table, change the PLAT-01 row from:
   `| PLAT-01     | Phase 1 | Pending  |`
   to:
   `| PLAT-01     | Phase 2 | Pending  |`

No other changes. Do not modify Phase 2's plan count, status, or any other content.
  </action>
  <verify>
1. Grep ROADMAP.md Phase 1 Requirements line -- must NOT contain PLAT-01.
2. Grep ROADMAP.md Phase 2 Requirements line -- must contain PLAT-01.
3. Grep REQUIREMENTS.md for PLAT-01 row -- must show Phase 2.
  </verify>
  <done>PLAT-01 is assigned to Phase 2 in both ROADMAP.md (requirements line) and REQUIREMENTS.md (traceability table). Phase 1 requirements list contains only PLAT-02, PLAT-06, CFG-01, CFG-04.</done>
</task>

</tasks>

<verification>
1. `pnpm run format:check` exits 0
2. `pnpm run build` still exits 0 (no regressions)
3. `pnpm run test` still passes all 21 tests (no regressions)
4. PLAT-01 appears in Phase 2 requirements in ROADMAP.md, not Phase 1
5. PLAT-01 traceability row in REQUIREMENTS.md shows Phase 2
</verification>

<success_criteria>
- format:check passes cleanly as a quality gate
- PLAT-01 correctly assigned to Phase 2 where MCP server is actually built
- No regressions in build, test, lint, or typecheck
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
