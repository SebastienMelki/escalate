---
phase: 05-escalation-intelligence
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/intelligence/audit.ts
  - src/intelligence/summary.ts
  - test/intelligence/audit.test.ts
  - test/intelligence/summary.test.ts
autonomous: true
requirements:
  - INTL-03
  - INTL-05

must_haves:
  truths:
    - "appendAuditEntry appends a single JSON line to the log file"
    - "appendAuditEntry creates parent directories if they do not exist"
    - "readAuditLog returns empty array when log file does not exist"
    - "readAuditLog parses all JSONL lines into AuditEntry objects"
    - "buildSessionSummary aggregates event counts by decision type"
    - "buildSessionSummary identifies notable events (timed_out, PostToolUseFailure)"
    - "buildSessionSummary returns zero-count summary when log is empty or missing"
    - "buildSummaryBlocks produces Block Kit formatted message from SessionSummary"
  artifacts:
    - path: "src/intelligence/audit.ts"
      provides: "Append-only JSONL audit log writer and reader"
      exports: ["appendAuditEntry", "readAuditLog", "AuditEntry"]
    - path: "src/intelligence/summary.ts"
      provides: "Session summary builder and Block Kit formatter"
      exports: ["buildSessionSummary", "buildSummaryBlocks", "SessionSummary"]
    - path: "test/intelligence/audit.test.ts"
      provides: "Unit tests for audit log write and read"
      min_lines: 50
    - path: "test/intelligence/summary.test.ts"
      provides: "Unit tests for session summary aggregation and Block Kit formatting"
      min_lines: 60
  key_links:
    - from: "src/intelligence/summary.ts"
      to: "src/intelligence/audit.ts"
      via: "readAuditLog provides entries for buildSessionSummary"
      pattern: "readAuditLog"
    - from: "src/intelligence/summary.ts"
      to: "src/slack/blocks.ts"
      via: "Block Kit formatting pattern (KnownBlock[])"
      pattern: "KnownBlock"
---

<objective>
Create the audit log writer/reader and session summary builder with TDD.

Purpose: Every escalation decision must be recorded in an append-only audit log for transparency. The session summary aggregates audit data into a Block Kit message sent at session end, giving the user visibility into what happened during the autonomous run.

Output: `src/intelligence/audit.ts`, `src/intelligence/summary.ts`, and comprehensive unit tests for both.
</objective>

<execution_context>
@/Users/sebastienmelki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastienmelki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/slack/blocks.ts
@src/intelligence/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit log module with TDD</name>
  <files>
    src/intelligence/audit.ts
    test/intelligence/audit.test.ts
  </files>
  <action>
RED phase: Create `test/intelligence/audit.test.ts` with tests for:
- `appendAuditEntry` writes one JSON line to a file (read back and JSON.parse succeeds)
- `appendAuditEntry` appends multiple entries on separate lines
- `appendAuditEntry` creates parent directories if they do not exist (use temp directory)
- `readAuditLog` returns `[]` when file does not exist
- `readAuditLog` returns parsed `AuditEntry[]` from a JSONL file with multiple entries
- `readAuditLog` skips empty lines (trailing newline robustness)
- Each AuditEntry has: timestamp, eventType, decision, reason fields

Use `os.tmpdir()` + random subdirectory for test isolation. Clean up after tests.

Run tests -- they MUST fail.

GREEN phase: Create `src/intelligence/audit.ts` with:
- `AuditEntry` interface: `{ readonly timestamp: string; readonly eventType: string; readonly toolName?: string; readonly filePaths?: readonly string[]; readonly decision: 'escalated' | 'auto_approved' | 'quiet_hours_suppressed' | 'timed_out'; readonly reason: string; readonly matchedRule?: string; readonly messagePreview?: string; readonly responseJson?: string; readonly escalationId?: string }`
- `appendAuditEntry(logPath: string, entry: AuditEntry): void` -- `mkdirSync(dirname(logPath), { recursive: true })` then `appendFileSync(logPath, JSON.stringify(entry) + '\n', 'utf-8')`
- `readAuditLog(logPath: string): AuditEntry[]` -- check `existsSync(logPath)`, return `[]` if missing. Read file, split by `\n`, filter empty lines, parse each as JSON. Return `AuditEntry[]`.

Run tests -- they MUST pass.

Use `node:fs` and `node:path` imports. Follow existing patterns: `readonly` on interfaces, explicit return types.
  </action>
  <verify>
    `npx vitest run test/intelligence/audit.test.ts` passes all tests.
    `npx tsc --noEmit` passes.
  </verify>
  <done>
    Append-only audit log writer creates JSONL entries. Reader handles missing files, empty logs, and multi-line logs. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Session summary builder with TDD</name>
  <files>
    src/intelligence/summary.ts
    test/intelligence/summary.test.ts
    src/intelligence/index.ts
  </files>
  <action>
RED phase: Create `test/intelligence/summary.test.ts` with tests for:
- `buildSessionSummary` returns zero-count summary when logPath does not exist
- `buildSessionSummary` returns zero-count summary when log is empty
- `buildSessionSummary` counts: 3 entries (1 auto_approved, 1 escalated, 1 timed_out) -> totalEvents=3, autoApproved=1, escalated=1, timedOut=1
- `buildSessionSummary` groups events by eventType in decisionsByType
- `buildSessionSummary` identifies notable events: timed_out and PostToolUseFailure entries
- `buildSummaryBlocks` returns a KnownBlock[] array with header "Session Summary"
- `buildSummaryBlocks` includes section block with stats (total events, auto-approved, escalated)
- `buildSummaryBlocks` includes notable events section when present
- `buildSummaryBlocks` handles empty summary gracefully (shows "No events recorded")

Create temp JSONL files with known audit entries for testing.

Run tests -- they MUST fail.

GREEN phase: Create `src/intelligence/summary.ts` with:
- `SessionSummary` interface: `{ readonly totalEvents: number; readonly autoApproved: number; readonly escalated: number; readonly quietHoursSuppressed: number; readonly timedOut: number; readonly decisionsByType: Record<string, number>; readonly notableEvents: readonly string[] }`
- `buildSessionSummary(logPath: string): SessionSummary` -- calls `readAuditLog(logPath)`, then reduces entries to aggregate counts. `decisionsByType` groups by `entry.eventType`. `notableEvents` filters entries where `decision === 'timed_out'` or `eventType === 'PostToolUseFailure'`, maps to descriptive strings.
- `buildSummaryBlocks(summary: SessionSummary): KnownBlock[]` -- returns Block Kit blocks:
  1. Header block: "Session Summary"
  2. Section block with mrkdwn: stats (Total: N, Auto-approved: N, Escalated: N, Suppressed: N, Timed out: N)
  3. If decisionsByType is non-empty: Section with breakdown per event type
  4. If notableEvents is non-empty: Section listing notable events
  5. If totalEvents is 0: Section saying "No escalation events recorded"
  Import `KnownBlock` from `@slack/types`.

Update `src/intelligence/index.ts` to also re-export from `./audit.js` and `./summary.js`.

Run tests -- they MUST pass.
  </action>
  <verify>
    `npx vitest run test/intelligence/summary.test.ts` passes all tests.
    `npx vitest run test/intelligence/` passes all intelligence tests.
    `npx tsc --noEmit` passes.
  </verify>
  <done>
    buildSessionSummary aggregates audit log into counts and notable events. buildSummaryBlocks produces Block Kit messages. Barrel export re-exports audit and summary modules. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run test/intelligence/` -- all intelligence tests pass (rules, quiet-hours, audit, summary)
- `npx tsc --noEmit` -- no type errors
- `npx eslint src/intelligence/ test/intelligence/` -- no lint errors
</verification>

<success_criteria>
- Audit log appends JSONL lines atomically and reads them back correctly
- Session summary correctly aggregates all decision types and identifies notable events
- Block Kit summary message is well-formatted with stats and notable events
- Missing/empty audit log handled gracefully without crashes
- All tests pass, types check, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-escalation-intelligence/05-02-SUMMARY.md`
</output>
