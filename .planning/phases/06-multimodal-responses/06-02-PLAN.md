---
phase: 06-multimodal-responses
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/slack/handlers.ts
  - src/slack/adapter.ts
  - src/slack/types.ts
  - src/slack/index.ts
  - src/server/http-bridge.ts
  - src/server/index.ts
autonomous: true
requirements: [MDIA-01, MDIA-02]

must_haves:
  truths:
    - "An emoji reaction on an escalation message resolves the escalation per the configured emoji-to-decision mapping"
    - "An unrecognized emoji reaction is silently ignored"
    - "Reactions on non-escalation messages are silently ignored"
    - "A voice note in an escalation thread is downloaded, transcribed, and resolves the escalation with the transcribed text"
    - "Non-audio file shares in escalation threads are silently ignored"
    - "Transcription failure posts a thread reply asking the user to type instead, leaving the escalation pending"
    - "Voice note feature disabled by default; emoji reactions enabled by default"
    - "If voice notes enabled but ESCALATE_OPENAI_API_KEY not set, startup warns clearly"
  artifacts:
    - path: "src/slack/handlers.ts"
      provides: "registerReactionHandler and registerFileShareHandler functions"
      contains: "registerReactionHandler"
    - path: "src/slack/adapter.ts"
      provides: "Reaction and voice note handling integrated into adapter lifecycle"
      contains: "handleReaction"
    - path: "src/slack/types.ts"
      provides: "Extended SlackAdapterCallbacks or new callback types"
    - path: "src/server/http-bridge.ts"
      provides: "TranscriptionProvider passed through options for voice note pipeline"
  key_links:
    - from: "src/slack/handlers.ts"
      to: "src/slack/adapter.ts"
      via: "onReaction callback"
      pattern: "onReaction"
    - from: "src/slack/handlers.ts"
      to: "src/slack/adapter.ts"
      via: "onFileShare callback"
      pattern: "onFileShare"
    - from: "src/slack/adapter.ts"
      to: "src/transcription/whisper.ts"
      via: "transcriptionProvider.transcribe()"
      pattern: "transcribe"
    - from: "src/slack/adapter.ts"
      to: "src/state/store.ts"
      via: "store.resolve() for reaction and voice responses"
      pattern: "store\\.resolve"
---

<objective>
Wire emoji reaction handling and voice note transcription into the Slack adapter, completing both multimodal response features end-to-end.

Purpose: This is the integration plan. Plan 01 created the types, config, and transcription provider. This plan wires them into the Slack event handling pipeline so reactions and voice notes actually resolve pending escalations. After this plan, both MDIA-01 and MDIA-02 are functional.

Output: Updated SlackAdapter with reaction and voice note handling, new Bolt event registrations, server startup validation for voice note prerequisites, and complete multimodal response resolution pipeline.
</objective>

<execution_context>
@/Users/sebastienmelki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastienmelki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multimodal-responses/06-RESEARCH.md
@.planning/phases/06-multimodal-responses/06-01-SUMMARY.md

@src/slack/adapter.ts
@src/slack/handlers.ts
@src/slack/types.ts
@src/slack/blocks.ts
@src/slack/index.ts
@src/server/http-bridge.ts
@src/server/index.ts
@src/types/escalation.ts
@src/config/schema.ts
@src/transcription/index.ts
@src/transcription/whisper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add emoji reaction handler and voice note handler to Slack adapter</name>
  <files>
    src/slack/handlers.ts
    src/slack/adapter.ts
    src/slack/types.ts
    src/slack/index.ts
  </files>
  <action>
1. **src/slack/handlers.ts** -- Add two new handler registration functions:

   a. Extend `SlackAdapterCallbacks` interface with two new callbacks:
      - `onReaction(channelId: string, messageTs: string, emoji: string, userId: string): void`
      - `onFileShare(threadTs: string, fileId: string, fileMimetype: string, fileSize: number, fileName: string, fileUrlPrivateDownload: string): void`

   b. Add `registerReactionHandler(app: App, callbacks: SlackAdapterCallbacks): void`:
      - `app.event('reaction_added', async ({ event }) => { ... })`
      - Guard: `if (event.item.type !== 'message') return;`
      - Call: `callbacks.onReaction(event.item.channel, event.item.ts, event.reaction, event.user)`

   c. Add `registerFileShareHandler(app: App, callbacks: SlackAdapterCallbacks): void`:
      - `app.message(async ({ message }) => { ... })` -- NOTE: This must be a SEPARATE app.message() listener from the existing registerMessageHandler. This listener specifically handles file_share subtypes.
      - Guard: Only handle messages with `subtype === 'file_share'` or messages that have a `files` array
      - Guard: Only handle thread replies (`thread_ts` must exist)
      - Guard: Ignore bot messages (`bot_id` present)
      - For each file in `message.files` (if present):
        - Check `file.mimetype?.startsWith('audio/')` -- skip non-audio files
        - Check `file.url_private_download` exists
        - Call: `callbacks.onFileShare(message.thread_ts, file.id, file.mimetype, file.size ?? 0, file.name ?? 'voice_note.webm', file.url_private_download)`
      - Use `await Promise.resolve()` for Bolt async handler signature requirement

2. **src/slack/adapter.ts** -- Add multimodal handling to SlackAdapter:

   a. Add imports:
      - `import type { TranscriptionProvider } from '../transcription/index.js'`
      - `import type { EscalateConfig } from '../config/schema.js'`

   b. Extend `SlackAdapterOptions` with:
      - `readonly config: EscalateConfig` (for emoji mapping and voice note config)
      - `readonly transcriptionProvider?: TranscriptionProvider` (optional, only when voice notes enabled)
      - `readonly botToken: string` (already exists, needed for file download)

   c. Store new fields in constructor:
      - `private readonly config: EscalateConfig`
      - `private readonly transcriptionProvider?: TranscriptionProvider` (use `| undefined` for exactOptionalPropertyTypes)
      - `private readonly botToken: string`
      - Build emoji mapping: `private readonly emojiMapping: Map<string, string>` from `config.multimodal.emojiReactions.mapping`

   d. Register new handlers in constructor callbacks object:
      - Add `onReaction: (channelId, messageTs, emoji, userId) => this.handleReaction(channelId, messageTs, emoji, userId)`
      - Add `onFileShare: (threadTs, fileId, mimeType, fileSize, fileName, downloadUrl) => this.handleFileShare(threadTs, fileId, mimeType, fileSize, fileName, downloadUrl)`
      - Call `registerReactionHandler(this.app, callbacks)` (only if `config.multimodal.emojiReactions.enabled`)
      - Call `registerFileShareHandler(this.app, callbacks)` (only if `config.multimodal.voiceNotes.enabled`)

   e. Add `private handleReaction(channelId: string, messageTs: string, emoji: string, _userId: string): void`:
      - Guard: `if (channelId !== this.channelId) return` -- only handle reactions in our channel
      - Guard: `const escalationId = this.tsToEscalation.get(messageTs); if (!escalationId) return` -- not a tracked escalation
      - Guard: `const decision = this.emojiMapping.get(emoji); if (!decision) return` -- unrecognized emoji
      - Resolve: `const resolved = this.store.resolve(escalationId, JSON.stringify({ type: 'reaction', emoji, actionId: decision }))` -- idempotent, returns false if already resolved
      - Guard: `if (!resolved) return`
      - Call resolver: `const resolver = this.responseResolvers.get(escalationId); if (resolver) { resolver({ type: 'reaction', emoji, actionId: decision, respondedAt: new Date() }); }`
      - Clean up maps: same pattern as handleAction (delete from escalationToTs, tsToEscalation, responseResolvers)

   f. Add `private handleFileShare(threadTs: string, fileId: string, mimeType: string, fileSize: number, fileName: string, downloadUrl: string): void`:
      - This is a synchronous dispatcher that fires and forgets the async pipeline
      - Guard: `const escalationId = this.tsToEscalation.get(threadTs); if (!escalationId) return`
      - Guard: `if (!this.transcriptionProvider) return` -- voice notes not configured
      - Guard: `if (fileSize > (this.config.multimodal.voiceNotes.maxFileSizeMb * 1024 * 1024)) return` -- too large
      - Fire and forget: `void this.processVoiceNote(escalationId, threadTs, fileId, mimeType, fileName, downloadUrl).catch((err: unknown) => { console.error('[escalate] Voice note processing failed:', err); })`

   g. Add `private async processVoiceNote(escalationId: string, threadTs: string, _fileId: string, mimeType: string, fileName: string, downloadUrl: string): Promise<void>`:
      - Download: `const audioBuffer = await this.downloadFile(downloadUrl)`
      - Transcribe: `const result = await this.transcriptionProvider!.transcribe(audioBuffer, mimeType, fileName)` -- use non-null assertion (we check in handleFileShare), or re-check
      - If transcription text is empty, throw to trigger catch in fire-and-forget
      - Resolve: `const resolved = this.store.resolve(escalationId, JSON.stringify({ type: 'voice', text: result.text }))`
      - Guard: `if (!resolved) return` -- already resolved by another handler
      - Call resolver: same pattern as handleAction but with `{ type: 'text', text: result.text, respondedAt: new Date() }` (use 'text' type for the resolver since downstream expects text-like response)
      - Clean up maps
      - Wrap the entire transcription in try/catch. On failure, post a threaded reply: `await this.app.client.chat.postMessage({ channel: this.channelId, thread_ts: threadTs, text: ':warning: Could not transcribe voice note. Please type your response instead.' })`. Do NOT resolve the escalation -- leave it pending.

   h. Add `private async downloadFile(url: string): Promise<Buffer>`:
      - `const response = await fetch(url, { headers: { Authorization: 'Bearer ' + this.botToken } })`
      - `if (!response.ok) throw new Error('Slack file download failed: ' + response.status)`
      - `return Buffer.from(await response.arrayBuffer())`

   i. Update `validateAndAnnounce()` to check voice note prerequisites:
      - After existing validation, if `this.config.multimodal.voiceNotes.enabled`:
        - Check `ESCALATE_OPENAI_API_KEY` or `OPENAI_API_KEY` env var exists
        - If not, `console.error('[escalate] WARNING: Voice notes enabled but ESCALATE_OPENAI_API_KEY not set. Voice note transcription will fail.')` -- warn but do not throw (graceful degradation)
        - If `this.transcriptionProvider` is undefined, `console.error('[escalate] WARNING: Voice notes enabled but no transcription provider configured.')`

3. **src/slack/types.ts** -- No new types needed beyond what handlers.ts adds to callbacks. Leave as-is unless a new type is needed for the file share data.

4. **src/slack/index.ts** -- Export the new handler registration functions:
   - Add `registerReactionHandler, registerFileShareHandler` to the handlers re-export line

  </action>
  <verify>
    Run `pnpm typecheck` -- no errors. Run `pnpm lint` -- no errors. Run `pnpm test` -- all existing tests pass. Check that handlers.ts exports registerReactionHandler and registerFileShareHandler.
  </verify>
  <done>
    SlackAdapter handles emoji reactions by looking up emoji in config mapping, resolving via store.resolve(). SlackAdapter handles voice notes by downloading audio, transcribing via TranscriptionProvider, resolving with transcribed text. Both features are conditional on config flags. Transcription failures post a fallback message without resolving the escalation. Startup validation warns if voice notes enabled without API key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire multimodal config and transcription provider into server startup</name>
  <files>
    src/server/index.ts
    src/server/http-bridge.ts
  </files>
  <action>
1. **src/server/index.ts** -- Update the server entrypoint to:
   a. After loading config and before creating the SlackAdapter, check multimodal config:
      - If `config.multimodal.voiceNotes.enabled`:
        - Read `process.env['ESCALATE_OPENAI_API_KEY'] ?? process.env['OPENAI_API_KEY']`
        - If API key exists, create `WhisperTranscriptionProvider` instance: `import { WhisperTranscriptionProvider } from '../transcription/whisper.js'`
        - Pass the provider to SlackAdapter options
      - If voice notes disabled or no API key, pass `undefined` for transcriptionProvider
   b. Pass the loaded `config` to SlackAdapterOptions (the new required field from Task 1)
   c. The rest of the server startup flow (MCP, HTTP bridge, Slack start, validate) stays the same

2. **src/server/http-bridge.ts** -- No changes needed. The multimodal features resolve entirely within the Slack adapter via the existing `store.resolve()` + `responseResolvers` pipeline. The HTTP bridge already reads resolved records via `GET /escalations/:id`.

3. **Verify the full data flow:**
   - Emoji reaction: Slack `reaction_added` event -> registerReactionHandler -> adapter.handleReaction -> tsToEscalation lookup -> emojiMapping lookup -> store.resolve() -> responseResolvers callback -> hook script poll picks up resolved status
   - Voice note: Slack `message` event with files -> registerFileShareHandler -> adapter.handleFileShare (sync) -> processVoiceNote (async fire-and-forget) -> downloadFile -> transcriptionProvider.transcribe -> store.resolve() -> responseResolvers callback -> hook script poll picks up resolved status
  </action>
  <verify>
    Run `pnpm typecheck` -- no errors. Run `pnpm lint` -- no errors. Run `pnpm test` -- all existing tests pass. Run `pnpm build` -- all entry points compile. Verify `dist/server/index.js` contains WhisperTranscriptionProvider import path.
  </verify>
  <done>
    Server startup creates WhisperTranscriptionProvider when voice notes enabled and API key available. Config is passed to SlackAdapter for emoji mapping resolution. Full data flow verified: reactions and voice notes resolve escalations through the existing pipeline. Build produces valid output. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes with zero errors
2. `pnpm lint` passes with zero errors
3. `pnpm test` -- all tests pass (no regressions)
4. `pnpm build` -- tsup compiles all entry points
5. `grep -r "reaction_added" src/slack/handlers.ts` confirms reaction handler registration
6. `grep -r "handleReaction" src/slack/adapter.ts` confirms reaction processing
7. `grep -r "processVoiceNote" src/slack/adapter.ts` confirms voice note pipeline
8. `grep -r "downloadFile" src/slack/adapter.ts` confirms file download method
9. `grep -r "WhisperTranscriptionProvider" src/server/index.ts` confirms provider wiring
</verification>

<success_criteria>
- Emoji reactions on escalation messages trigger resolution via configurable emoji-to-decision mapping
- Reactions on non-escalation messages or with unmapped emoji are silently ignored
- Voice notes in escalation threads are downloaded, transcribed, and resolve the escalation
- Non-audio file shares are silently ignored
- Transcription failures post a fallback thread reply without resolving
- Voice note feature requires explicit opt-in (disabled by default)
- Server startup warns if voice notes enabled without API key
- Both features use the existing store.resolve() + responseResolvers pipeline
- All existing tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-multimodal-responses/06-02-SUMMARY.md`
</output>
