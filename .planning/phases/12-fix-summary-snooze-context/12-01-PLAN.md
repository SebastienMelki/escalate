---
phase: 12-fix-summary-snooze-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/on-task-completed.ts
  - scripts/on-stop.ts
  - hooks/hooks.json
  - src/server/http-bridge.ts
  - scripts/lib/output-helpers.ts
autonomous: true
requirements:
  - INTL-05
  - SLCK-02
  - SLCK-03
must_haves:
  truths:
    - "Session summary actually sends to Slack before on-task-completed.ts exits"
    - "Session summary actually sends to Slack before on-stop.ts exits"
    - "Snooze button appears alongside Approve/Deny for PermissionRequest and PreToolUse events"
    - "Clicking Snooze produces a deny decision with 'Snoozed by user' message"
    - "File paths extracted from tool input appear in Slack escalation messages"
    - "PreToolUse hook process is not killed mid-poll by Claude Code"
  artifacts:
    - path: "scripts/on-task-completed.ts"
      provides: "Async main with awaited requestSummary"
      contains: "await requestSummary"
    - path: "scripts/on-stop.ts"
      provides: "Awaited requestSummary instead of fire-and-forget"
      contains: "await requestSummary"
    - path: "src/server/http-bridge.ts"
      provides: "Snooze action in buildActionsForEvent, filePaths in context"
      contains: "snooze"
    - path: "scripts/lib/output-helpers.ts"
      provides: "isSnoozeDecision helper and snooze handling in output builders"
      contains: "isSnoozeDecision"
    - path: "hooks/hooks.json"
      provides: "PreToolUse timeout bumped to 320s"
      contains: "320"
  key_links:
    - from: "scripts/on-task-completed.ts"
      to: "scripts/lib/bridge-client.ts:requestSummary"
      via: "await instead of void"
      pattern: "await requestSummary"
    - from: "src/server/http-bridge.ts:buildActionsForEvent"
      to: "scripts/lib/output-helpers.ts:isSnoozeDecision"
      via: "snooze actionId flows from button to output handler"
      pattern: "snooze"
    - from: "src/server/http-bridge.ts:context"
      to: "src/slack/blocks.ts"
      via: "filePaths in EscalationRequest.context renders as Files: block"
      pattern: "filePaths"
---

<objective>
Fix four gaps identified in the v1.0 third audit: (1) session summary never sends because process.exit kills pending async I/O, (2) Snooze button missing from escalation actions, (3) filePaths not passed to Slack message context, and (4) PreToolUse hook timeout races with poll deadline.

Purpose: Close the final 3 requirement gaps (INTL-05, SLCK-02, SLCK-03) and 1 flow gap to complete v1.0 specification.
Output: All six surgical fixes applied across 5 files with passing tests and build.
</objective>

<execution_context>
@/Users/sebastienmelki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastienmelki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-summary-snooze-context/12-RESEARCH.md
@scripts/on-task-completed.ts
@scripts/on-stop.ts
@scripts/lib/bridge-client.ts
@scripts/lib/output-helpers.ts
@src/server/http-bridge.ts
@hooks/hooks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix session summary async flow and PreToolUse timeout buffer</name>
  <files>
    scripts/on-task-completed.ts
    scripts/on-stop.ts
    hooks/hooks.json
  </files>
  <action>
    Fix 1a — on-task-completed.ts: Convert `main()` from sync to async. Change signature from `function main(): void` to `async function main(): Promise<void>`. Change `void requestSummary(port).catch(() => {})` to `await requestSummary(port).catch(() => {})`. Replace the `try { main(); } catch { process.exit(0); }` wrapper with `main().catch(() => process.exit(0))` to match on-stop.ts pattern.

    Fix 1b — on-stop.ts: Change `void requestSummary(port).catch(() => {})` on line 37 to `await requestSummary(port).catch(() => {})`. The function is already async, so this is a one-word change (`void` -> `await`). Do NOT change anything else in this file.

    Fix 1c — hooks.json: Change PreToolUse timeout from 300 to 320. This gives a 20s buffer over the 300s poll deadline (FETCH_TIMEOUT_MS=10s + POLL_INTERVAL_MS=2s + startup/margin). Do NOT change any other timeout values.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm TypeScript compiles.
    Run `npx vitest run` to confirm all existing tests still pass.
    Run `npx tsup` to confirm build succeeds.
    Verify hooks.json PreToolUse timeout is 320 (not 300).
  </verify>
  <done>
    on-task-completed.ts has `await requestSummary(port).catch(() => {})` before `process.exit(0)` with async main().
    on-stop.ts has `await requestSummary(port).catch(() => {})` instead of `void requestSummary(port).catch(() => {})`.
    hooks.json PreToolUse timeout is 320.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Snooze button, handle snooze response, and pass filePaths to context</name>
  <files>
    src/server/http-bridge.ts
    scripts/lib/output-helpers.ts
  </files>
  <action>
    Fix 2a — http-bridge.ts buildActionsForEvent: In the PermissionRequest/PreToolUse case (lines 82-87), add a third action `{ id: 'snooze', label: 'Snooze' }` after the Deny button. No style property (default button appearance). Do NOT add Snooze to Stop or PostToolUseFailure events.

    Fix 2b — http-bridge.ts context object: In the POST /escalations handler where the context object is built (lines 293-296), add `...(filePaths.length > 0 ? { filePaths } : {})` after the toolName spread. This uses the conditional spread pattern already established in the codebase for exactOptionalPropertyTypes.

    Fix 2c — output-helpers.ts snooze handling: Add a module-private `isSnoozeDecision()` helper function (matching the existing `isApprovalDecision` pattern):
    ```typescript
    function isSnoozeDecision(response: ParsedResponse): boolean {
      return (
        (response.type === 'action' || response.type === 'reaction') &&
        response.actionId === 'snooze'
      );
    }
    ```
    In `buildPermissionRequestOutput()`, add a snooze check AFTER the approval check and BEFORE the default deny:
    ```typescript
    if (response && isSnoozeDecision(response)) {
      return JSON.stringify({
        hookSpecificOutput: {
          hookEventName: 'PermissionRequest',
          decision: { behavior: 'deny', message: 'Snoozed by user via Escalate' },
        },
      });
    }
    ```
    In `buildPreToolUseOutput()`, add a snooze check AFTER the approval check and BEFORE the default deny:
    ```typescript
    if (response && isSnoozeDecision(response)) {
      return JSON.stringify({
        hookSpecificOutput: {
          hookEventName: 'PreToolUse',
          permissionDecision: 'deny',
          permissionDecisionReason: 'Snoozed by user via Escalate',
        },
      });
    }
    ```

    Fix 2d — output-helpers.ts tests: Add test cases in the existing output-helpers test file:
    - Test that buildPermissionRequestOutput with snooze action returns deny with "Snoozed by user via Escalate" message.
    - Test that buildPreToolUseOutput with snooze action returns deny with "Snoozed by user via Escalate" reason.
    - Test that buildStopOutput does NOT handle snooze (snooze should not exist for Stop, but verify no crash if received).
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm TypeScript compiles.
    Run `npx vitest run` to confirm all tests pass (including new snooze tests).
    Run `npx tsup` to confirm build succeeds.
    Grep for 'snooze' in http-bridge.ts to confirm button added.
    Grep for 'filePaths' in http-bridge.ts context object to confirm wiring.
  </verify>
  <done>
    buildActionsForEvent returns Approve/Deny/Snooze for PermissionRequest and PreToolUse.
    Snooze response produces deny with "Snoozed by user via Escalate" in both PermissionRequest and PreToolUse output builders.
    filePaths are passed to EscalationRequest.context when non-empty.
    All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npx vitest run` — all tests pass, including new snooze tests
3. `npx tsup` — build succeeds
4. `grep -n 'await requestSummary' scripts/on-task-completed.ts scripts/on-stop.ts` — both files show await
5. `grep -n 'snooze' src/server/http-bridge.ts scripts/lib/output-helpers.ts` — snooze wired through
6. `grep -n 'filePaths' src/server/http-bridge.ts` — filePaths in context object
7. `grep -n '"timeout": 320' hooks/hooks.json` — PreToolUse buffer applied
</verification>

<success_criteria>
- Session summary sends before process.exit in both on-task-completed.ts and on-stop.ts
- Snooze button appears for PermissionRequest and PreToolUse (not Stop, not PostToolUseFailure)
- Snooze response maps to deny with descriptive message
- filePaths flow from http-bridge.ts context to Slack Block Kit rendering
- PreToolUse hooks.json timeout (320s) exceeds poll deadline (300s) by safe margin
- All existing tests continue to pass, new snooze tests added
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-summary-snooze-context/12-01-SUMMARY.md`
</output>
