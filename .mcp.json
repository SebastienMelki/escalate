{
  "mcpServers": {
    "escalate": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server/index.js"],
      "env": {
        "CLAUDE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
